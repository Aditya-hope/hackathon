# Test-only dependencies. Not needed to run the app itself -
# see requirements.txt for that. Installed separately in CI
# so a broken test dependency can never block a production build.
pytest>=8.0,<9.0
httpx>=0.27,<1.0
